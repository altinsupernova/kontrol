# CLAUDE.md — AI Assistant Guide for `kontrol`

## Repository Overview

This is the **kontrol** repository (`altinsupernova/kontrol`). The project is in its initial stage — this CLAUDE.md serves as the foundational guide for AI assistants working in this codebase.

> **Note:** Update this file as the project evolves. Add language-specific conventions, architecture details, and workflow commands as they are established.

## Project Structure

```
kontrol/
├── CLAUDE.md          # This file — AI assistant guide
└── (project files to be added)
```

## Development Workflow

### Git Conventions

- **Default branch:** `main` (to be established with the first commit)
- **Feature branches:** Use descriptive names prefixed by category (e.g., `feat/`, `fix/`, `refactor/`)
- **Commit messages:** Use concise, imperative-mood messages (e.g., "Add user authentication", "Fix null pointer in parser")
- **Pull requests:** Include a summary of changes and link to relevant issues

### Build & Test Commands

<!-- Update these as the project tooling is set up -->
```
# TBD — add build, test, lint, and format commands here
```

## Coding Conventions

<!-- Update these once the language/framework is chosen -->
- Keep code simple and readable
- Prefer explicit over implicit
- Write tests for new functionality
- Avoid premature abstractions

## Key Files

| File | Purpose |
|------|---------|
| `CLAUDE.md` | AI assistant guide (this file) |

## Notes for AI Assistants

- Always read relevant source files before proposing changes
- Run tests after making changes (once test infrastructure exists)
- Keep changes focused — one logical change per commit
- Do not add unnecessary dependencies or abstractions
- When in doubt, ask the user for clarification
